class Enemy{
    constructor(x,y){
        this.position = createVector(x,y);
        this.velocity = createVector(0,0);
        this.acceleration = 0;
        this.topSpeed = 1;
        this.r = 10;
    }

    
}